# 更新日志 · 祭晾屋 image storage

> 本地视觉打标 · 图片/视频整理工具（EVA02 离线模型）

---

## v1.3 — 2026-09-07

### 修复
- **移动文件夹后标签消失**：移动文件时标签改为跟随文件记到新路径，重扫（含子文件夹）后标签照常保留。
- **移动后同名文件看起来重复**：缩略图卡片改显示相对子目录路径（如 `OUT/0.jpg`），不同子文件夹里的同名文件不再混成"重复"。
- **移动失败的文件不再从列表消失**：之前只要进了移动队列，即使移动失败也会被移出列表；现在只移除真正移动成功的文件。
- 视频点击恢复为**软件内预览**（上一版误改成调系统播放器）。

### 优化
- 扫描时对文件列表去重，防止符号链接等导致同一文件被列两次。

---

## v1.2 — 2026-09-07

### 新增
- **视频支持**：可扫描、预览、打标视频文件（`.mp4 .mov .mkv .webm .avi .wmv .m4v .flv`）。
  - 打标：按时长等分抽 5 帧，逐帧识别后合并去重。
  - 缩略图：取视频中间帧生成。
  - 内置 ffmpeg，无需另装。
- **标签分类**：所有标签自动归入 14 个语义大类——
  **人物 / 衣服 / 配饰 / 身体 / 发型发色 / 眼睛 / 表情 / 姿势动作 / 场景 / 构图 / 物品 / 食物 / 生物 / 其他**。
  - 侧栏新增「分类筛选条」，点某个分类只看该类的标签。
  - 每个分类有专属颜色：分类胶囊与标签前的圆点用颜色区分。
  - 「全部」视图下每个标签后标注 `· 分类`。
- **视频流式传输**：视频按 Range 分块边读边发，点开更快。

### 优化
- 标签分类覆盖率大幅提升：漏分到「其他」的比例从约 **19% → 3%**。
  - 英文标签改用「下划线边界 token 精确匹配」，避免 `eye` 误命中 `eyebrow`、`ass` 误命中 `glass` 之类。
  - 中文标签先反查英文名再分类（比中文子串更准）；部分中文别名本身是英文（如 `SM`/`3P`/`NTR`）也统一走反查。
  - 修复带连字符关键词（`see-through`、`upside-down` 等）永远匹配不上的问题。
- 视频在软件内预览：关闭查看器保留进度，重开同一视频不重复加载。

### 打包
- 安装程序**不再内置模型**（约 1.2GB），改为**首次运行自动下载**（hf-mirror 优先，huggingface 官方源兜底）。

---

## v1.1 — 2026-09-05

### 新增
- **Windows 安装程序**（Inno Setup 6 打包，含桌面 / 开始菜单快捷方式）。
- **模型自动下载**：缺少模型时启动即自动拉取。
- **差分图检测**：dHash + 汉明距离归组「同姿势变体」，星形聚类避免链式误合并，阈值 1–64 可调。
- **GIF 预览**：内置播放器，进度条可拖、画面实时跟帧。
- **标签多选**：点多个标签 = 同时包含（AND）过滤。

### 优化
- **深色 SaaS 风格 UI** 整体重设计。
- 大图库性能：分块渐进渲染、缩略图懒加载、shimmer 只对近屏元素生效、打标进行中不再整页重建网格。
- 新增「选择文件夹」按钮。

---

## v1.0 — 早期版本

### 新增
- 从桌面版（customtkinter）迁移到 **Web 版**（FastAPI 后端 + 浏览器前端）。
- 打标引擎仅保留 **EVA02-Large（ONNX + DirectML）**，移除 Ollama；GPU 加速，失败自动回退 CPU。
- 中文标签字典：danbooru 中文优先，e-hentai 兜底映射。
- 品牌更名 **「祭晾屋 image storage」**。
- 打标数据集中存到 `tagdata/`，与图库分离，换文件夹 / 重装不丢标签。
